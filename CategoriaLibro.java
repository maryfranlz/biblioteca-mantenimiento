/**
 * Categorías posibles de un libro.
 *
 * Al ser un enum, sólo se pueden usar estos valores: el compilador
 * evita que alguien escriba una categoría inventada o mal escrita.
 */
public enum CategoriaLibro
{
    FICCION,
    NOVELA,
    AVENTURA,
    CIENCIA,
    HISTORIA,
    TECNOLOGIA;
}
